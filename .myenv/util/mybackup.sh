#!/bin/bash

# Req
#	accept: naming tag, filenames, dirnames, filelists, command eval
#	exclude: by excludelist, wildcard


# Design
#	target name include: date, naming tag, hostname, 

# TODO
#	how to support command eval and store it?
#	possible to just zip, not need copy?
#	- for single filename, dirname, seems ok. Otherwise, need copy

