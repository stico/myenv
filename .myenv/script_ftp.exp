#!/usr/bin/expect

# This script using the expect to force the ftp client set to binary. Will use the user/password set in ~/.netrc file, so not auth interaction here.

spawn ftp "[lindex $argv 0]" "[lindex $argv 1]"
expect "*Logged on"
expect "*ftp>"
send "binary\r"
expect "*ftp>"
interact
